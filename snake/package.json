{
  "name": "snake",
  "version": "1.0.0",
  "description": "a snake game for studies",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=5000 --entry-file=./index.html --no-browser"
  },
  "author": "djanilson alves djanilson",
  "email": "project@hostname.com",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
